<%= simple_form_for [@job, @resume] do |f| %>
  <%= f.input :content %>
  <%= f.input :attachment %>
  <%= f.submit "Submit", data: {disable_with: "Submiting..."}, class: "btn btn-primary" %>
<% end %>
